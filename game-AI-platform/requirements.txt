# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
python-dotenv>=1.0

# LLM SDKs (only one is needed, but both are listed for flexibility)
openai>=1.40.0
anthropic>=0.34.0

# Utilities
starlette>=0.38.0
